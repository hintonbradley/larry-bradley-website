(function() {
	//We're adding data to the angular module so it must begin with angular.module
		angular.module('LarryBradley')
		.controller('SlideshowController', ['$scope','$state','$log', function($scope, $state, $log) {
		}]);
	}());